"use strict"; // treat all Js code as newer version
// alert(3+3)  //we are using nodejs, not brower

console.log(3 + 3);

let name = "Rishi"; // String
let age = 19; // number
let IsLogined = true; // false //  = >boolean

// => data type in js

//  number => 2 to power 53

// bigint => use for huge data like trading , etc

// string => prefered "to use  double  quotes "

// boolean => true / false

// null => stand alone value (representation of empty)

// undefined => mean value is not defined

// symbol => to identify the unique

// ------------- Object -----------
console.log(typeof undefined); // undefined
console.log(typeof null); // object
